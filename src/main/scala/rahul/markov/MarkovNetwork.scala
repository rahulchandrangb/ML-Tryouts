package rahul.markov
import rahul.pgm.FactorTable

import scala.collection.mutable.ListBuffer

class MarkovNetwork(networkName: String) {
  val nodes = ListBuffer[MarkovNode]()
  def insertNewNode(name: String, node: MarkovNode, connectedNodes: List[MarkovNode]) = {
    
  }
}

object MarkovNetwork {

}